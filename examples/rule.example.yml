id: architecture-review
inject:
  - design-review
  # Prefix a catalog Bundle explicitly; unprefixed targets remain Skills.
  - bundle:architecture-review
match:
  - exact:
      - review this design
  - regex:
      - '^design review:'
  - contains:
      - architecture
    not:
      - contains:
          - no context
